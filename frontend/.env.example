# Copy this file to .env  (cp .env.example .env)

# In development: leave this EMPTY — Vite proxies /api → localhost:3000 automatically.
# In production (Vercel): set this to your deployed backend URL.
# Example: VITE_API_BASE_URL=https://wind-forecast-monitor.onrender.com
VITE_API_BASE_URL=
